# Additive, hash-pinned wheels allowed into a release overlay on top of a
# previously scanned worker image. Empty on purpose: the vLLM base carries the
# model stack, and the project itself installs from source.
